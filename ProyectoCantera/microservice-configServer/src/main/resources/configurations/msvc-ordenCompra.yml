server:
  port: 9095

spring:
  application:
    name: msvc-ordenCompra
  datasource:
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
    url: jdbc:sqlserver://localhost:1433;databaseName=LA_CANTERA;trustServerCertificate=true
    username: codel
    password: 123

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka